<div class = "NavBar">
    <div class = "antialiased bg-gray-100">
        <div class = "w-full text-gray-700 bg-white" >
            <div class = "NavBar">
            <div class = "flex flex-col max-w-screen-xl px-4 mx-auto md:items-center md:justify-between md:flex-row md:px-6 lg:px-8">
                <div class = "flex flex-row items-center justify-between p-4">
                    <%= link_to "Marvel", root_path, class: "custom-link" %>  
                |
                    <%= button_to "Request box", requests_path, class: "custom-link" %>
                |
                    <%= link_to "New arrivals", root_path, class: "custom-link" %>
                </div>
                <% if current_page?(root_path) %>
                <div class = "stick-second-right">
                    <%= link_to "Orders", root_path, class: "custom-link" %>
                |
                    <%= link_to "wishlist", root_path, class: "custom-link" %>
                |
                    <%= link_to "Cart", root_path, class: "custom-link" %>
                </div>
                <% end %>
                <nav class = "stick-to-right">
                    <% if user_signed_in? %>
                        <%= link_to "Hi #{current_user.email}", edit_user_registration_path, class: "btn" %><br> <br> <%= link_to 'sign out', destroy_user_session_path, :method => :delete, class: "btn user" %>
                    <% else %>
                        <%= link_to 'sign in', new_user_session_path, class:"btn" %> <br><br>
                         <%= link_to "sign up", new_user_registration_path, class:"btn" %>
                        
                    <% end %>
                </nav>
            </div>
            </div>
        </div>
    </div>
</div>