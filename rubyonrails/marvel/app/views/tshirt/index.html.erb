<ul>
    <% @tshirts.each do |tshirt| %>
        <li><%=  tshirt.color %></li>
        <li><%= tshirt.character %></li>
        <li><%= tshirt.size %></li>
        <li><img src = "<%= tshirt.img_url %>"></li>
        <li>
            <%= link_to "🗑", tshirt_path(tshirt) , method: :delete %> 
        </li>
        <br>
    <% end %>
</ul>